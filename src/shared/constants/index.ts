/**
 * Fichier d'export centralisé pour toutes les constantes de l'application
 * 
 * Permet d'importer toutes les constantes depuis un seul endroit :
 * 
 * @example
 * ```ts
 * import { STORAGE_KEYS, SUPPORTED_IMAGE_EXTENSIONS, getSpringTransition } from '@/shared/constants';
import { logger } from '../utils/logger';
 * ```
 */

export * from "./storage";
export * from "./fileTypes";
export * from "./animations";
